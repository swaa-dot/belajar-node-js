//standard library

import os from 'os';

console.info(os.platform()); // Outputs the platform of the operating system
console.info(os.cpus()); // Outputs the CPU architecture4
console.info("memori");
console.info(os.freemem()); // 